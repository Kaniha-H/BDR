{{ form_start(form) }}

    <div class="form-group">
        {{ form_label(form.name) }}
        {{ form_widget(form.name) }}
        {{ form_errors(form.name) }}
        {{ form_help(form.name) }}
    </div>

    <div class="form-group">
        {{ form_label(form.description) }}
        {{ form_widget(form.description) }}
        {{ form_errors(form.description) }}
        {{ form_help(form.description) }}
    </div>

    <div class="form-group">
        {{ form_label(form.release_in) }}
        {{ form_widget(form.release_in) }}
        {{ form_errors(form.release_in) }}
        {{ form_help(form.release_in) }}
    </div>

    <div class="form-group">
        {{ form_label(form.page_number) }}
        {{ form_widget(form.page_number) }}
        {{ form_errors(form.page_number) }}
        {{ form_help(form.page_number) }}
    </div>

    <div class="form-group">
        {{ form_label(form.price) }}
        {{ form_widget(form.price) }}
        {{ form_errors(form.price) }}
        {{ form_help(form.price) }}
    </div>

    <div class="form-group">
        {{ form_row(form.illustration) }}
    </div>

    <button type="submit" class="btn btn-primary">{{ btn_label | default("Créer un produit") }}</button>
{{ form_end(form, {'render_rest': false}) }}